VITE_API_BASE_URL=http://localhost:8080
# Generate with `npx web-push generate-vapid-keys` — must match the backend's VAPID_PUBLIC_KEY.
# Left blank, subscribeToPush() no-ops and the app falls back to in-tab notifications only.
VITE_VAPID_PUBLIC_KEY=
